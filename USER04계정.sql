CREATE TABLE TEMP(
NO NUMBER);
INSERT INTO TEMP VALUES(1);

SELECT * FROM TEMP;

SELECT * FROM SCOTT.EMP;

CREATE VIEW VW2
AS
SELECT * FROM TEMP;


PRINT NUM;
SELECT :NUM FROM DUAL;--바인드 변수는 PL/SQL이나 다른 문에서 실행시 앞에 :을 붙여야한다.

VARIABLE NUM NUMBER;
DECLARE
    NUM1 NUMBER(3) :=100;
BEGIN
    :NUM := NUM1 *2;
END;
/
